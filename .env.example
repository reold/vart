# Browser-facing API URL used by a production/static build.
VITE_API_URL=https://vart.reold.workers.dev

# API target used only by the local Vite same-origin proxy.
# Change to http://localhost:8787 when running the Worker locally.
VITE_API_TARGET=https://vart.reold.workers.dev

# Set by the GitHub Pages workflow for https://reold.github.io/vart/.
BASE_PATH=
